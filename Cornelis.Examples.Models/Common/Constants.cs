namespace Cornelis.Examples.Models.Common
{
    /// <summary>
    /// We use a library of common frases to avoid having to type a string variables.
    /// This leaves code cleaner, less prone to typing errors and easier to change at a later
    /// date.
    /// </summary>
    public static class Constants
    {
        public static string User = "User";
    }
}